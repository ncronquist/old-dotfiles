# CDS Engine Test
# This command sets up the engine project with a test package so that the engine can
# be run and tested locally

cdset() {
    git clean -xdf
    lerna bootstrap
    cp -r ~/Documents/projects/nicks-test-packages/graphql-profile ~/Documents/projects/cds/engine/packages/prod-host/node_modules/@cds/
}

