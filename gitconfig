[push]
    default = current
[color]
    ui = auto
[core]
    excludesfile = ~/.gitignore
    autocrlf = input
    pager = less -+$LESS -R
[commit]
    template = ~/.gitmessage
[fetch]
    prune = true
[rebase]
    autosquash = true
[user]
    email = nick@ncronquist.com
    name = Nick Cronquist
[filter "lfs"]
    clean = git-lfs clean -- %f
    smudge = git-lfs smudge -- %f
    process = git-lfs filter-process
    required = true
[url "ssh://git@github.concur.com"]
    insteadof = https://github.concur.com
